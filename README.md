# CNN_Classification
A deep learning project that uses a Convolutional Neural Network (CNN) built with TensorFlow and Keras to classify images of cats and dogs. The model is trained on the Cats vs Dogs dataset with automatic preprocessing, training, and accuracy evaluation.
